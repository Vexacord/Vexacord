/**
 * VexaCord - custom Discord app with a great modification built-in!
 * (C) 2024 VexaCord
 * 
 * When you contribute, we and you have copyright to your changes.
 */

/**
 * Window constant for faster writing.
 */
const win = window;

/**
 * Console constant for faster writing
 */
const con = win.console;

/**
 * Document constant for faster writing
 */
const doc = win.document;

/**
 * VexaCord developers.
 * Add yourself here, if you created a plugin. The value of your name must be your GitHub username.
 */
const devs = {
    VexaCord: 'VexaCord'
}

/**
 * Export it
 */
export {win, con, doc, devs};